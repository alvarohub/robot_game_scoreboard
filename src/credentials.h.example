#pragma once
// ── WiFi credentials ─────────────────────────────────────────
// Copy this file to  credentials.h  (same directory) and fill in
// your real values.  credentials.h is listed in .gitignore so it
// will NOT be committed to version control.

#define WIFI_SSID     "your_wifi_ssid"
#define WIFI_PASSWORD "your_wifi_password"
